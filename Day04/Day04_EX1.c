#include <stdio.h>

int main() {
	
	char a = 'A';

	if (a == 'A') {
		printf("C언어 반입니다.");
	} else if (a == 'B') {
		printf("파이썬 반입니다.");
	} else if (a == 'C') {
		printf("자바 반입니다.");
	}

	return 0;
}