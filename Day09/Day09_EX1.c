#include <stdio.h>

int main() {
	int num = 4;

	// 1 : 참 - 0 : 거짓
	if (num > 3) { // - 참 , -> 1
		// 조건이 참이여야
		// 여기 어떻게 들어오냐면
	}

	if (3 <= num && num <= 5) {
		printf("num이 3 이상 5 이하 입니다.");
	}

	if (5 >= num >= 3) {
		printf("num이 5 이하 3 이상 입니다.");
	}

	return 0;
}